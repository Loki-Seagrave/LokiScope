# LokiScope
An SSH/FTP Brute force automation script for Linux. This script uses a combination of msfconsole and msfvenom, and automates the payload creation, listener setup, payload hosting via HTTPS, Self-signed SSL Certificate generation, and payload execution. Please take a look at the options by running the script without arguments to see adjustability.
